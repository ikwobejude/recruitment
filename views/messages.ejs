
    <% Object.keys(messages).forEach(function (type) { %>
      <% messages[type].forEach(function (message) { %>
        <div class="alert alert-<%= type %> dark alert-dismissible fade show" role="alert">  <%= message %>  </div>
      <% }) %>
    <% }) %>